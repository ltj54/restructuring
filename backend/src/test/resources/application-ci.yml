# backend/src/test/resources/application-ci.yml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_ci
    username: restructuring
    password: restructuring
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:migration

logging:
  level:
    root: INFO
